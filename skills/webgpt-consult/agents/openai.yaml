interface:
  display_name: "webgpt-consult"
  short_description: "Verified GPT-5.6 Sol second opinions through ChatGPT Web"
  default_prompt: "Use /webgpt-consult for a verified GPT-5.6 Sol second opinion. Use independent for a fresh review, continuation only when the current Codex session has a verified Web conversation binding, and branch when that bound thread needs context relief."

policy:
  allow_implicit_invocation: false
