interface:
  display_name: "GPT 5.6 Sol Consult"
  short_description: "Verified GPT-5.6 Sol reviews with project-aware continuity"
  default_prompt: "Use $webgpt-consult for a verified GPT-5.6 Sol second opinion, reusing the matching project workstream when appropriate."

policy:
  allow_implicit_invocation: false
