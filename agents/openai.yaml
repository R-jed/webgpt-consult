interface:
  display_name: "GPT 5.6 Sol Consult"
  short_description: "Verified GPT-5.6 Sol second opinions with local state"
  default_prompt: "Use /webgpt-consult for a verified GPT-5.6 Sol second opinion. Prefer a fresh independent review unless this is a clear follow-up to one existing consultation."

policy:
  allow_implicit_invocation: false
