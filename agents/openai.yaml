interface:
  display_name: "GPT 5.6 Sol Consult"
  short_description: "Verified GPT-5.6 Sol second opinions through ChatGPT Web"
  default_prompt: "Use /webgpt-consult for a verified GPT-5.6 Sol second opinion. Use independent for a fresh review, continuation only for the current clear review thread, and branch when that thread needs context relief."

policy:
  allow_implicit_invocation: false
